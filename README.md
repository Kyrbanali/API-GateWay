# API-GateWay
После createuser класть в канал его id (структура типа воркер пуш айди)
В пакете воркер есть горутина которая ждет записи в этот канал и читает, этот воркер будет делать запрос с помощью данных пользока в яндекс и получать первые 3 ссылки которые он выдает

Сделать так, чтобы можно было указать сколько горутин может читать из канала